# Changes

```
npm install -g grunt-cli nodemon serverless

apm install language-docker language-ansible vim-mode-plus \
  linter linter-eslint linter-pylama editorconfig \
  emmet file-icons fonts merge-conflicts terminal-plus \
  nord-atom-ui nord-atom-syntax ariake-dark-syntax
```
