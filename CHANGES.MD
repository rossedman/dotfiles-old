# Changes

```
remove .aliases
npm install -g azure-cli grunt-cli nodemon serverless
pip install boto3 pytest pytest-cov

brew install awscli aws-shell jq m-cli
brew cask uninstall hyper
brew cask install atom

apm install language-docker language-ansible vim-mode-plus \
  linter linter-eslint linter-pylama editorconfig \
  emmet file-icons fonts merge-conflicts terminal-plus

apm install nord-atom-ui nord-atom-syntax ariake-dark-syntax

```
